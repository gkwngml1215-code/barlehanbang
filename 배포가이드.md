# 바를정한방병원 안면마비·재활센터 홈페이지 배포 · 어드민 설치 순서 (GitHub → Cloudflare Pages)

울산 정한의원·수원 바를정한의원과 같은 방식입니다. **소스는 GitHub, 배포는 Cloudflare Pages(Git 연결)**, 어드민은 같은 저장소의 **`admin` 브랜치**를 별도 Cloudflare Pages 프로젝트로 올리고 `_worker.js` 로 아이디·비밀번호를 겁니다. 파일을 직접 드래그해서 올리는 Direct Upload 는 쓰지 않습니다.

| 항목 | 값 |
|---|---|
| GitHub 저장소 | `gkwngml1215-code/bareuljung-face` (Private) — 1단계에서 생성 |
| 홈페이지 | `main` 브랜치 → Cloudflare Pages 프로젝트 `bareuljung-face` → 도메인 (확정 후 연결) |
| 어드민 | `admin` 브랜치 → Cloudflare Pages 프로젝트 `bareuljung-face-admin` → `https://bareuljung-face-admin.pages.dev` |
| 도메인 | 미정. 임시값 `https://face.barrrjung-hospital.co.kr` (`admin/config.json` siteUrl) — 확정되면 변경 |
| 내 PC 폴더 | `E:\★병의원 작업★\★홈페이지 제작\바를정한방병원\bareuljung-face` |

이 사이트는 칼럼 원고(JSON)에서 페이지를 **Cloudflare 가 빌드할 때** 만듭니다. 그래서 홈페이지 프로젝트에 빌드 명령이 들어갑니다 (2단계 표 참고).

---

## 1단계 — GitHub 저장소 만들기

로컬 git 저장소는 이미 만들어져 있고 첫 커밋이 들어 있습니다. GitHub 에 올리는 방법:

1. https://github.com/new → Repository name `bareuljung-face` → **Private** → [Create repository] (README 등은 추가하지 않음)
2. 사이트 폴더에서 아래 실행

```
git remote add origin https://github.com/gkwngml1215-code/bareuljung-face.git
git push -u origin main
node tools/sync-admin.js
```

3. GitHub 에 `main` 브랜치(홈페이지)와 `admin` 브랜치(어드민)가 보이면 완료.

> Claude 에게 "GitHub 에 올려줘" 라고 하면 위 명령을 대신 실행합니다.

---

## 2단계 — 홈페이지 프로젝트 만들기 (Cloudflare Pages ← GitHub main)

1. https://dash.cloudflare.com 로그인 → 왼쪽 **Workers & Pages**
2. **[Create]** → 위쪽 **Pages** 탭 → **[Connect to Git]**
3. **GitHub** 선택 → 저장소 목록에 **`bareuljung-face`** 가 없으면 [Configure GitHub App] 에서 추가로 체크
4. 저장소 **`bareuljung-face`** 선택 → **[Begin setup]**
5. 아래처럼 입력합니다 (**가장 중요한 단계**)

   | 항목 | 넣을 값 |
   |---|---|
   | Project name | `bareuljung-face` (주소가 `bareuljung-face.pages.dev`) |
   | Production branch | **`main`** |
   | Framework preset | **None** |
   | Build command | **`node tools/build.js`** |
   | Build output directory | **`dist`** |

6. **[Save and Deploy]** → 1~2분 뒤 "Success" 가 뜨면 `https://bareuljung-face.pages.dev` 에서 홈페이지가 보입니다.

> Build command 나 output directory 를 비워두면 칼럼 페이지가 만들어지지 않고 어드민·도구 파일이 그대로 노출됩니다. 꼭 위 표대로 넣어 주세요.

7. (권장) 프로젝트 → **Settings** → **Builds & deployments** → **Branch control** → **Preview branch: None** → Save.
8. 도메인 확정 전이라도 `admin/config.json` 의 `siteUrl` 을 `https://bareuljung-face.pages.dev` 로 바꿔 커밋하면 canonical·sitemap 이 실제 주소로 나옵니다. 도메인을 붙인 뒤 다시 바꿉니다.

---

## 3단계 — 도메인 연결

도메인이 정해지면 (예: `face.barrrjung-hospital.co.kr` 서브도메인 또는 새 도메인):

- **서브도메인**인 경우: 본 사이트 도메인의 DNS 에 CNAME `face → bareuljung-face.pages.dev` 를 추가하고, Cloudflare Pages 프로젝트 → **Custom domains** → 해당 주소 추가. (본 도메인 DNS 가 Cloudflare 가 아니면 CNAME 만 추가하면 됩니다)
- **새 도메인**인 경우: 울산 정한의원 배포가이드 3단계와 같이 도메인을 Cloudflare 에 추가 → 네임서버 변경 → Custom domains 에 `www.도메인` 과 `도메인` 추가 → Redirect Rules 로 하나로 모으기.
- 연결 후 `admin/config.json` siteUrl, `tools/scheduler-worker/wrangler.toml` SITE_URL 을 실제 주소로 바꿔 커밋합니다.

---

## 4단계 — 어드민 프로젝트 만들기 (Cloudflare Pages ← GitHub admin)

1. **Workers & Pages** → **[Create]** → **Pages** → **[Connect to Git]** → 저장소 **`bareuljung-face`** → **[Begin setup]**
2. 입력:

   | 항목 | 넣을 값 |
   |---|---|
   | Project name | `bareuljung-face-admin` |
   | Production branch | **`admin`** ← 반드시 admin (main 아님) |
   | Framework preset | **None** |
   | Build command | (비워둠) |
   | Build output directory | **`/`** |

3. **[Save and Deploy]**
4. 프로젝트 → **Settings** → **Builds & deployments** → **Branch control** → Production branch `admin`, **Preview branch: None** → Save
5. **Settings** → **Variables and Secrets** → **[Add]** 로 2개 추가

   | 이름 | 값 | 종류 |
   |---|---|---|
   | `ADMIN_USER` | 원하는 아이디 | **Secret** |
   | `ADMIN_PASS` | 원하는 비밀번호 | **Secret** |

6. **[Save]** → **Deployments** 탭 → 최신 배포 **[...]** → **[Retry deployment]**
7. 시크릿 창(Ctrl+Shift+N)으로 `https://bareuljung-face-admin.pages.dev` 접속 → **아이디·비밀번호 입력창이 뜨면 잠금 성공**

| 이런 화면이 나오면 | 원인 | 해결 |
|---|---|---|
| 입력창 없이 바로 열림 | `_worker.js` 가 적용 안 됨 | Build output directory 가 `/` 인지 확인 후 재배포 |
| `설정 오류: ... ADMIN_USER ...` | 환경 변수 이름이 틀림 | 5번 표와 한 글자씩 대조 |
| 맞는 비번인데 계속 물어봄 | 재배포를 안 함 | 6번(Retry deployment) |
| 홈페이지가 보임 | Production branch 가 main | 2번을 admin 으로 |

---

## 5단계 — GitHub 접속 키 만들기 (어드민 안에서 쓰는 키)

1. https://github.com/settings/personal-access-tokens → **[Generate new token]**

   | 항목 | 넣을 값 |
   |---|---|
   | Token name | `바를정 안면마비센터 어드민` |
   | Expiration | **No expiration** |
   | Repository access | **Only select repositories** → **`bareuljung-face`** |
   | Permissions → Repository permissions → **Contents** | **Read and write** |

2. **[Generate token]** → `github_pat_` 로 시작하는 문자열을 복사
3. `https://bareuljung-face-admin.pages.dev` → 아이디·비밀번호 → 관리 화면 **왼쪽 메뉴 맨 아래 "🔑 GitHub 접속 키"** 칸에 붙여넣고 **[저장]**
4. 상태가 "연결됨 ✓" 으로 바뀌고 칼럼 목록에 **초기 칼럼 6편**이 보이면 끝입니다.

**폰·노트북을 잃어버렸다면** GitHub 같은 화면에서 그 키를 **Delete** 하세요.

---

## 6단계 — 예약 발행 스케줄러 (선택)

미래 날짜로 예약한 글을 그날 자동 공개하려면 Cloudflare Worker 를 하나 올립니다.

1. Cloudflare Pages 프로젝트 `bareuljung-face` → **Settings** → **Builds & deployments** → **Deploy hooks** → [Add deploy hook] → 이름 `scheduler`, 브랜치 `main` → 생성된 주소 복사
2. `tools/scheduler-worker/` 폴더에서

```
npx wrangler login
npx wrangler secret put DEPLOY_HOOK     (1번에서 복사한 주소 붙여넣기)
npx wrangler deploy
```

3. 매일 한국 시간 09:05·12:05 에 예약 글이 있으면 자동으로 다시 빌드됩니다.

---

## 이제 이렇게 씁니다

1. 어디서든 `https://bareuljung-face-admin.pages.dev` → 아이디·비밀번호 → (처음 한 번) 접속 키
2. **새 칼럼 쓰기** → 작성 → **관련 진료 페이지** 선택 → **저장하고 공개**
3. 확인창에서 **새 글/수정, 상태, 주소, 관련 페이지, 올라가는 사진 개수**를 보고 **확인**
4. "GitHub 저장 완료 · 홈페이지 반영 중…" → 1~2분 뒤 "홈페이지 반영 완료 ✔" → **미리보기**
5. 사진은 올리기 전에 자동으로 웹용 크기(긴 변 1600px)로 줄어듭니다.

- 목록은 항상 **GitHub 에 올라가 있는 글** 기준입니다. 다른 PC·폰에서도 똑같이 보입니다.
- 글 주소(슬러그)가 이미 있는 글과 겹치면 저장이 멈춥니다. 다른 글을 덮어쓰는 일은 없습니다.
- **삭제**는 [삭제] 버튼 → 확인창을 눌렀을 때만 됩니다.
- 임시저장은 GitHub 에 저장되지만 홈페이지에는 보이지 않습니다.
- **예약 발행**: 발행일을 미래 날짜로 두고 저장하고 공개 → 그날 오전 9시쯤 자동 공개 (6단계 스케줄러 필요).

## 홈페이지 설정 (어드민 메뉴)

어드민 왼쪽 **🏥 홈페이지 설정** 에서 병원명·센터명·대표원장·전화·주소·병상수·의료진수·위도경도·진료시간·채널 링크(카카오·유튜브·본 사이트·네이버 플레이스·블로그·인스타)·검색 설정·공지 띠를 고치고 **저장하고 홈페이지에 반영** 을 누르면 1~2분 뒤 전체 페이지와 구조화 데이터에 반영됩니다. 값은 `site.json` 에 저장됩니다.

## 홈페이지 · 어드민을 고쳤을 때

- **홈페이지 문구·사진** (칼럼 이외): 이 PC 에서 고친 뒤 `main` 에 푸시 → Cloudflare 가 1~2분 뒤 자동 배포.
- **메뉴**: `components/header.html` 하나만 고치면 빌드 때 전 페이지에 들어갑니다.
- **어드민 화면**: `admin/` 폴더를 고친 뒤 `node tools/sync-admin.js` → `admin` 브랜치에 올라가고 자동 배포.
- 배포 기록·되돌리기: Cloudflare 프로젝트 → **Deployments** 탭

**`admin/_worker.js` 는 절대 지우거나 이름을 바꾸지 마세요.** 이 파일이 아이디·비밀번호 잠금입니다.

---

## 배포 후 할 일 (구현가이드 11장)

- 네이버 서치어드바이저 · 구글 서치콘솔에 사이트 등록 → 소유확인 코드를 어드민 홈페이지 설정에 입력 → `sitemap.xml` 제출, 네이버는 `column/rss.xml` 도 제출
- 본 사이트(barrrjung-hospital.co.kr)·네이버 플레이스·블로그·유튜브 설명란에 새 사이트 링크 추가 (백링크)
- Google 리치결과 테스트 · Schema.org Validator 로 JSON-LD 오류 0건 확인, Lighthouse 90점 이상
- 실제 사진·로고 원본·비급여 금액 교체 (README 6번)
